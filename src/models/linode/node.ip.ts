export interface INodeIpAddress {
    address: string
    gateway: string
    subnet_mask: string
    prefix: number
    type: string
    public: boolean
    rdns: string
    linode_id: boolean
    region: string
}